<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-950 text-white flex items-center justify-center min-h-screen">

  <div class="bg-gray-800 p-8 rounded-xl shadow-lg w-full max-w-md text-center">
    <h1 class="text-3xl font-extrabold mb-6 text-blue-400">Login to Your Account</h1>
    
    <form action="/login" method="POST" class="flex flex-col space-y-4 text-left">
      <input 
        class="bg-gray-700 p-3 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" 
        type="email" 
        placeholder="Email" 
        name="emailId" 
        required
      />

      <input 
        class="bg-gray-700 p-3 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" 
        type="password" 
        placeholder="Password" 
        name="password" 
        required
      />

      <button 
        type="submit" 
        class="bg-blue-600 hover:bg-blue-700 transition-all duration-200 text-white font-semibold py-3 rounded-md"
      >
        Login
      </button>
    </form>

    <p class="mt-6 text-gray-400">Don't have an account?</p>
    <a 
      href="/signup" 
      class="inline-block mt-2 bg-green-600 hover:bg-green-700 transition-all duration-200 text-white font-semibold py-2 px-4 rounded-md"
    >
      Create New Account
    </a>

    <!-- Optional: Profile button -->
    <p class="mt-6 text-gray-400">Already logged in?</p>
    <a 
      href="/profile" 
      class="inline-block mt-2 bg-purple-600 hover:bg-purple-700 transition-all duration-200 text-white font-semibold py-2 px-4 rounded-md"
    >
      Go to Profile
    </a>
  </div>

</body>
</html>
