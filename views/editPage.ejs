<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-950 text-white flex items-center justify-center min-h-screen">

  <form action="/update/<%= post._id %>" method="POST" class="bg-gray-900 p-8 rounded-lg shadow-lg w-full max-w-md space-y-4">
    <h1 class="text-2xl font-bold text-blue-400 text-center mb-2">Update Your Post</h1>

    <textarea
      name="content"
      rows="3" 
      placeholder="<%= post.content %>" 
      class="w-full p-4 bg-gray-800 border border-gray-700 rounded-md resize-none focus:outline-none focus:ring-2 focus:ring-blue-500"
      required
    ></textarea>

    <button 
      type="submit" 
      class="w-full bg-yellow-600 hover:bg-yellow-700 text-black font-semibold py-3 rounded-md transition duration-200"
    >
      Update Post
    </button>
  </form>

</body>
</html>
